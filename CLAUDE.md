# 補助金プロジェクト - Claude向けメモ

このファイルは Claude Code が自動で読み込む、プロジェクト横断のコンテキストファイル。

## プロジェクト概要

デジタル化・AI導入補助金（旧IT導入補助金）の申請書類を、ヒアリングシート・PDF資料・Excel賃金台帳から AI で自動生成するツール。
本番運用は Streamlit Cloud で動作。

## ディレクトリ構成

```
補助金/
├── app.py                  Streamlit エントリポイント
├── run.py                  CLI エントリポイント
├── hojokin/                コア処理パッケージ
│   ├── ai_extractor.py    Claude API 呼び出し（PDF/CSV/Excel 抽出）
│   ├── config.py          設定・テンプレートマッピング
│   ├── pipeline.py        申請書作成・給与計算のオーケストレーション
│   ├── template_filler.py Excel 書込み
│   ├── wage_reader.py     賃金台帳の決定論パーサー
│   ├── pdf_reader.py      PDF テキスト抽出
│   ├── google_drive.py    Drive API クライアント
│   ├── google_sheets.py   Sheets API クライアント
│   └── ...
├── gas/                    Google Apps Script（管理表自動化）
├── docs/                   運用マニュアル・設計ドキュメント
├── ツール/                 申請テンプレート・ヒアリングシート原本
└── credentials/            Google サービスアカウント鍵（gitignore）
```

## 主要設定

- 環境変数: `.env` 参照（`.env.example` を雛形として）
- 主な変数: `CLAUDE_API_KEY`, `GOOGLE_CREDENTIALS_PATH`, `MANAGEMENT_SHEET_ID`, `USE_AI_WAGE_EXTRACTION`
- Google Sheet/Drive の各種 ID は `.env` で管理。コードにハードコードしない。

## 業務体制・運用ルール

### 連絡窓口
- **賃上げ申請関連の指示・依頼は坂平さん（窓口）経由で受ける**
- 1次振り返りMTG（2026-05-14）で混乱回避のため一本化決定
- 山田さん・村上さんから個別に依頼が来た場合も、坂平さんに集約してから動く

### 賃金台帳の回収方針
- 顧客への依頼は **原則 Excel / CSV 形式**（フォーマット統一によりAPI節約・自動化精度向上）
- PDF しか出ない場合の処理経路（優先順。**2026-07-21 の3次申請振り返りで従来の「ツール優先」から「一旦ローカル標準」へ変更**）:
  1. **手元の Claude Code ＋ `wagebook-convert` Skill（ローカル、標準）**。精度優先（3次の実測で、ツール出力に月額ズレ・ローカル変換は原本一致。CC は最も精度の高いモデルを使える）。手書き/画像PDF も対応。Skill 本体は `.claude/skills/wagebook-convert/` で git 管理、配布は引き継ぎパック（hojokin-handoff.zip）同梱に一本化（v0.2.85〜。単体 ZIP・ツールUIの導入リンクは v0.2.86 で廃止）。**出力は「従業員別明細」＋「原本転記」シート群（表紙＋ページ別 `原本転記_pN`。PDF全ページ・全項目の転記・確認用＝通勤手当等の関数計算・加点判定への転用可）の構成**（2026-07-21 決定・v0.2.91〜。集計行だけの転記は仕様違反＝2026-07-26 全項目・ページ別化、verify の FLAG-R で機械検知）。ツールのリーダーはシート名「原本転記」プレフィックスの可視シートを全経路で読み飛ばす（`wage_reader.TRANSCRIPT_SHEET_PREFIX`／`_visible_worksheets`）。プレフィックスを変えるならスキル側 SKILL.md §4.1.2 と必ず両側同期
  2. **ツール側「賃金台帳の作成」タスク**（速さ優先時・CC 環境が無い/不慣れな場合の代替）。Document AI + Sonnet 4.6 で PDF → ツール規格 Excel に自動変換。Sonnet 画像フォールバックは無効化済み（精度面で劣るため）
- 変換用テンプレート: `ツール/賃金台帳テンプレート.xlsx`（B5-T5 ヘッダー、項目：No / 氏名 / 雇用形態 / 月平均時間 / 時給 / 1〜12月 / 年間通勤手当(S) / 年間賞与(T)）
- **ツール経路とスキル経路は独立実装だが、出力規格は同一**（同じ `賃金台帳テンプレート.xlsx` 形式・同じ R215/R216 ルール）。**出力規格（テンプレ列・補助金ルール）を変更/追加したら、必ず両方を差分チェックして同じく直す**（片方だけ直すとスキル産物がツールに正しく読み込まれない。例: v0.2.33 の T列「年間賞与」追加はツール/スキル両テンプレ＋ZIP を同期した）

### 賃上げ計画値の丸め（2026-07-21〜）
- 申請書の給与支給総額 計画値（1〜3年目）は**常に切り上げ**。前年の表示整数からの逐次切り上げ（y1=ceil(基準×1.03), y2=ceil(y1×1.03), y3=ceil(y2×1.03)）で、申請ポータルの伸び率表示が対基準年・年次間とも 3.0% を割らない（四捨五入時代は1円差で「2.9%」表示になる事故があった）。実装は `pipeline.py` の計画値算出と給与支給総額計算書の ROUNDUP 数式（v0.2.91）。

### 通勤手当の扱い（2026-07-27〜・運用変更）
- **通勤手当は課税・非課税を問わず給与支給総額（R216）から控除する**（坂平さんとの MTG で決定）。従来は「非課税分のみ控除／課税扱いの通勤手当は公募要領 p.10 の課税対象経費なので算入」だった。公募要領の文言とは異なるが、R216 が小さくなる＝賃上げ計画の基準額が下がる安全側の運用で、過大計上ではないので違反にはならない。事務局への確認は取っていない（将来照会する機会があれば確認する論点）。
- 🔴 **控除する額は「月列に採用した値に実際に含まれている通勤手当の年額」**。通勤手当の全額ではない。非課税処理されている台帳の課税支給合計にはそもそも通勤手当が入っていないので、そこから引くと削られるのは基本給・残業手当（配布テスト3案件で実測した事故）。判定は `課税支給額 ＝ Σ支給項目（通勤手当を含む）` が成り立つかで機械的に決まる。判定表は [.claude/skills/wagebook-convert/SKILL.md](.claude/skills/wagebook-convert/SKILL.md) §3.1。
- 通勤手当の額が台帳から特定できない場合（諸手当に合算・総支給と課税支給の差しか印字なし）は**引かずに報告に明記**する。顧客に内訳を強制的に出させる運用にはしない（内訳を出していない会社もある）。引かない状態は公募要領どおりなので違反にはならないが、**無言の未控除は許さない**。
- **適用は今後の新規案件から**。既存の成果物は作り直さない。既存案件を再変換するときは既存台帳の方式を維持し、どちらの方式で作ったかを方式スタンプ（B2注記・変換メモ・完了報告）に残す。**効果報告（1〜3年目の実績）も基準年度と同じ方式で算出する**（方式が混ざると伸び率が歪む）。
- 機械ゲート: `review/verify_transcript.py` の T5 が控除漏れ（含まれているのに S列が空）と二重控除（非課税処理済みなのに S列に値）の両方を FAIL で止める。ツール側は S列の減算で月額が0円化した場合に W-WAGE-004 で警告する（0円月があるとその従業員が R215/R216 から丸ごと落ちるため）。

### 申請枠 × 法人格は4通り（2026-08-10〜）

- 当社が扱うのは **通常枠×法人 / 通常枠×個人事業主 / インボイス枠×法人 / インボイス枠×個人事業主** の4通り。申請書テンプレもヒアリング様式も4通りそれぞれ別物で、`config.get_mapping()` のキーは `通常枠_2026` / `通常枠_個人_2026` / `インボイス枠_2026` / `インボイス枠_個人_2026`。
- **通常枠は個人事業主も申請できる**（公募要領の補助対象者に法人格の限定なし）。それ以前の資料の「通常枠は法人のみ」は誤り。
- **通常枠個人の正本は原本管理担当者が Drive で管理する「【原本/法人】企業名_通常枠/個人2026」**（2026-08-13 作成・独自レイアウト。タイトルの「原本/法人」は改名依頼中）。リポジトリ原本 `ツール/【原本_個人】企業名_通常枠_個人2026.xlsx` は、そのエクスポートに `scripts/patch_genpon_tsujo_kojin_from_drive.py` が機械的修正（シート9案内文の番地・フリガナ/事業所所在地の配線・AI()/IMPORTXML キャッシュ残骸・法人前提注記3件）だけを当てたコピー（2026-08-17〜）。**原本を手で直さず、Drive 側更新時は再エクスポート→パッチ再実行**。旧 `build_genpon_tsujo_kojin.py`（素材連結方式）は引退。
- `config.MAPPING_2026_TSUJO_KOJIN` は `scripts/gen_mapping_tsujo_kojin.py` が**新原本の B/A 列ラベル引き当てで機械生成**する。原本・ヒアリングシートの行が変わったら生成し直して差し替える。検証は `scripts/verify_genpon_tsujo_kojin.py`（構造約190項目・config 整合込み）。
- 通常枠個人だけセル番地が他と違う（ツール名 C75・シート9連動3項目 C153〜C155・プロンプト C76・事業内容 C77）。**枠だけでセル番地を決めない**。書き込み前の W-TPL-001 ラベル検証ゲートが版ズレ・取り違えを止める。
- ヒアリング様式の判別: 2つの個人様式は基本情報の上半分が完全に同じ。`check_docs.py` の `HEARING_FORMS` は r31 の設問文言（通常枠=「業務上の課題」／インボイス=「時間がかかっている業務」）と r84/r85 で見分ける（2026-08-17 再実測。指紋を変えるときは4様式のスコア行列が対角3・他1以下になることを確認）。

### 申請スケジュール
- 次回2次申請スケジュール（暫定）：
  - 推奨送客期限 6/1（月）
  - 疎明期限 6/3
  - 資料提出期限 6/8
  - 申請締切 6/15（月）

### 残タスクの参照先
- 2次申請に向けた改善タスクは [`docs/TODO_2次申請改善.md`](docs/TODO_2次申請改善.md) に集約

### 加点判定（賃上げ加点・項目14/15）の運用（v0.2.39〜）
- **2工程**：①実行タスク「加点判定用賃金台帳の作成」で専用台帳を作り → ②実行タスク「加点判定」でその台帳から判定。
- **専用台帳が必要**（R215/R216 用の標準賃金台帳は不可）。加点は「**時間換算給与＝基本給÷月間所定労働時間**（残業・通勤・精皆勤・家族手当を除く所定内賃金）」を**暦月固定**（令和6年10月〜令和7年9月＋交付申請直近月）で見るため。標準台帳は総支給ベースで基本給・所定労働時間・暦月固定列を持たず、正社員が判定からこぼれる。
- 専用台帳テンプレ：`ツール/加点判定用賃金台帳テンプレート.xlsx`（シート『加点判定用明細』、列見出しは YYYY-MM 暦月固定）。リーダー/ライターのレイアウト定数は `wage_reader.BWL_*` が単一の真実。S〜AD/AE 列＝**月別労働時間**（任意。時給制等の月変動に対応、空欄の月は E列フォールバック、旧形式台帳も後方互換。v0.2.72〜）。未入力だと E列固定で時間換算給与が歪むため、judge が法定最賃割れを検知して警告する。
- 都道府県は会社で1つ。役員は判定母数から除外。**交付申請月**の入力が加点②に必須（直近月＝申請月の前月）。
- ①の提出シートは**申請枠で1枚**（通常枠＝補助率引上げ・加点措置①用[データ開始19] / インボイス・セキュリティ枠＝加点措置①用[データ開始18]）。`fill_bonus_sheet_1` が「No」見出し行+2 で動的検出。
- 提出シート①②の生成は**原本コピー＋対象セルの値のみ差し替え**（`hojokin/xlsx_surgical.py`、v0.2.89〜）。openpyxl 再保存だと公式様式の図形・記入例画像・秘密度ラベルが消えてフォーマットが変わるため、原本 ZIP を丸ごと温存し sheet XML の入力セルだけを書き換える（人間が原本に直接入力したのと同等の成果物）。`fullCalcOnLoad` 付与で開いた時に判定式・MIN式が再計算される。
- **抽出精度が補助率2/3 を左右**するため、生成台帳は提出前に人手確認（最賃ギリギリ層は原本照合）。所定労働時間が AI で取れない月給制正社員は E列が空欄 → 会社の月平均所定労働時間を手入力。
- 詳細は [`docs/補助金_実務知識ベース.md`](docs/補助金_実務知識ベース.md)「加点措置・補助率引上げ」節。
- 注：ツールの自動生成は **PDF 入力に未対応**（Excel 入力用として残置）。**PDF 台帳の案件は `katen-judge` スキルでローカル完結**（2026-07-21 決定 → v0.2.91 で整備済み・パック収載）。`.claude/skills/katen-judge/` ＝ PDF→専用台帳の変換ルール（SKILL.md）＋ `review/run_katen_judge.py`（hojokin と同一エンジンで判定・公式様式①②を surgical 生成。openpyxl のみで動作）。実行チェックリストは `docs/チェックリスト_加点判定.md`。パック側の hojokin はビルド毎にリポジトリからコピー＝同期ドリフトなし。出力規格＝加点判定用テンプレを変えたら両系統を同期する思想は標準台帳と同じ。

## 開発時の注意

### コード規約
- マジックナンバーは定数化、上部にまとめる
- `sys.stdout.reconfigure(encoding='utf-8')` をスクリプト先頭に
- 日本語パスは `pathlib.Path.iterdir()` で解決
- Excel に書き込む文字列の先頭に `=` を入れない（数式と誤認）

### 個人情報・機密情報の取り扱い（重要）
- **顧客企業名・顧客従業員氏名・年収・連絡先など、特定可能な個人情報や顧客情報をリポジトリにコミットしない**
- 作業ログや調査メモは、抽象化した形（「顧客企業A」「担当者」等）で記述する
- 顧客実データ（ヒアリングシート、賃金台帳、給与明細等）は `.gitignore` 配下に置く
- Google Sheet ID・Drive フォルダ ID・Webhook URL 等は環境変数 / Apps Script Properties に逃がす

### 本番デプロイ
- Streamlit Cloud と GitHub の `main` ブランチが連携。push で自動再デプロイ。
- ロールバックは `git revert` または Streamlit Cloud secrets での機能フラグ切替
- `USE_AI_WAGE_EXTRACTION=false` で AI 抽出を旧経路（決定論パーサー）に戻せる
- **Streamlit Cloud のログのタイムスタンプ（`[HH:MM:SS]`）は UTC**。日本時間（JST）はそれ +9時間。ログ時刻を読むときは +9 して換算する（例：ログ `[04:23]` ＝ JST 13:23）

## テスト方針（暗黙ルール）

- **テスト実行時に Anthropic API は絶対に呼ばない**（API課金ゼロが原則）。
  `_debug/test_*.py` は `StubExtractor` / `MagicMock` / Sonnet サブエージェント代用で動くこと
- AI モデルは **2系統（v0.2.49〜）**：**抽出（登記簿/PL/見積/賃金台帳）= Sonnet 4.6**（`CLAUDE_MODEL` 既定 `claude-sonnet-4-6`）、
  **文章生成（事業内容255字を含む判断生成・短縮・増量）= Opus 4.8**（`WRITING_MODEL` 既定 `claude-opus-4-8`）。
  「文章を書く処理だけ Opus」が方針。抽出は Sonnet+画像(Vision)維持（レイアウト命の財務書類で実績・回帰回避）。
  テストは `StubExtractor`/`MagicMock` で API を呼ばないため、この既定変更の影響を受けない。
- 大規模変更後は `_debug/` 配下のテストを全件回し、回帰なしを確認してから push

## ロールバック・運用フラグ

| フラグ | デフォルト | 役割 |
|---|---|---|
| `USE_AI_WAGE_EXTRACTION` | `true` | 賃金台帳の AI 抽出 ON/OFF。`false` で決定論パーサーのみ |
| `CLAUDE_MODEL` | `claude-sonnet-4-6` | 抽出（画像/PDF）用モデル |
| `WRITING_MODEL` | `claude-opus-4-8` | 文章生成（事業内容）用モデル。`claude-sonnet-4-6` にすれば生成も Sonnet に戻る |
| `USE_STRUCTURED_PL_EXTRACTION` | `true` | PL抽出を構造分解（基本PL/販管費部/原価部の3呼出）にする。`false` で旧1呼出方式 |
| `USE_PL_PAGE_INVENTORY` | `true` | 構造分解PL抽出で4ページ以上のPDFを先にページ分類し関連ページのみ送信（複数年度混入防止・トークン削減）。3ページ以下はスキップ |
| `USE_PROMPT_CACHING` | `true` | 賃金台帳PDF抽出で Anthropic Prompt Caching を使う（固定の指示部をキャッシュし入力トークン費用を削減） |
| `USE_PDF_TEXT_PREPROCESSING` | `false` | 賃金台帳PDFを画像でなくテキスト化してSonnetに渡す。コスト削減狙いだが精度低下リスクで既定OFF（案件ごと確認後に有効化） |

## 毎年の定期更新（最低賃金＝10月改定）

地域別最低賃金は **毎年10月に全国改定** される。加点①②はこの最賃で判定するため、改定を取りこぼすと古い値で資料を出す事故になる（2026-07-07 ある加点②案件：加点②シートの内蔵マスタが R6＝大阪1114円のままで、直近月 R7＝1177円が反映されず先方指摘）。

**毎年、改定発効（10月）後すみやかに以下を更新・確認する：**
- `config.MIN_WAGE_MAP`（改定後＝最新値）と `wage_reader.MIN_WAGE_R6`（前年度＝改定前。加点①の下限判定に使う）の2本を、公式「賃金状況報告シート」内蔵マスタと全47都道府県で突合。
- 加点②の公式テンプレ `補助金加点/加点措置②用.xlsx` の内蔵「最低賃金マスタ」が最新改定を反映しているか（公式が更新版を出していれば差し替え。現状は R6 値内蔵）。
- 加点②は **基準月＝令和7年7月（R6期）** と **直近月＝申請月前月（R7期）** で適用最賃が変わるが、公式②シートは両期を1つのマスタで引く設計。期の食い違いに注意。

**最賃欄は加点②の判定には使われない**（判定は時給MINの2時点差 ≥ 63円のみ）。ただし表示が古い/実態と違うと事務局の疑義を招く。派遣業など勤務地が事業所所在地と異なる場合は、比較する地域最賃を勤務地基準にすべきか要確認（上記の加点②案件で論点化）。

## 関連ドキュメント

- `docs/マニュアル_書類作成.md` — Streamlit/Claude Code を使った書類作成手順
- `docs/運用マニュアル.md` — 案件管理から書類作成までの運用フロー
- `docs/設計_API自動化.md` — 完全自動化バージョンの設計案
- `docs/案件メモ/` — 個別案件の調査ログ（テンプレートのみ。実案件メモは gitignore）
- **`docs/補助金_制度リファレンス.md`** — **制度そのもの（申請枠・補助率・加点項目の枠別マトリクス・補助対象者/みなし大企業・必要書類・コンソーシアム・スケジュール・採択実績）で迷ったらここ**。全項目に出典ラベル（`[公式]`/`[帰結]`/`[運用]`/`[経験則]`/`[要確認]`）が付いており、一次資料で確認した事実と伝聞が分けてある。会議で聞いた制度の話は、まず議事に `[経験則]` で残し、一次で裏が取れたらここへ昇格させる（昇格ルールは同ファイル §0）。鮮度管理は `python scripts/check_seido_freshness.py`（公式ダウンロードページの更新日を `docs/制度一次情報/sources.json` と突合。標準ライブラリのみ・API課金なし。各申請回の頭と毎年10月の最賃改定時に回す）。
- **`docs/補助金_実務知識ベース.md`** — **R215/R216 や賃金台帳の取り扱いに迷ったらまずここを参照**。公募要領の定義（給与支給総額・役員除外・賞与の扱い）、賃金台帳の構造解釈（段1/段2/役員ブロック）、Codex 調査結果のサマリを集約。鮮度管理あり。
- **`.claude/skills/wagebook-convert/`** — **PDF賃金台帳をツール用 Excel に変換するための CC Skill**。`SKILL.md` 本体に手順、`reference/` に役員正規化と公募要領要点、`checklists/` に検証チェックリスト、`templates/` に賃金台帳テンプレート、`examples/` に抽象化サンプル変換例を同梱。**xlsxの月列には「直近事業年度の12ヶ月分」を入れる（暦年ではない）** ← 落とし穴。配布は ZIP（`scripts/build_skill_zip.py` で生成）→ Drive 経由で他担当者へ。CC が文脈で自動発火、または `/wagebook-convert` で明示起動。
- **`.claude/skills/case-docs-check/`** — **案件フォルダ（Drive/ローカル）の必要書類チェック用 CC Skill**（v0.2.90〜）。申請書作成・加点判定に必要な書類の有無と中身（対象年度・発行日・形式・テンプレ規格・ヒアリング様式取り違え）を1回の発動でチャット一括報告する。Drive 読み取り専用・成果物ファイルなし・途中質問なし。「必要書類チェックして」で自動発火、`/case-docs-check` で明示起動。Drive アクセスは各自の Google Drive コネクタ（MCP）経由（SA 不要）＝引き継ぎパック同梱で他担当者も使える。**分類・必須判定の定数は `hojokin/pipeline.py`（FileDetector）と `app.py` からの独立コピー＝原本を変えたらスキル側 `review/check_docs.py` も同時に直す**（`scripts/check_docscheck_sync.py` が handoff ZIP ビルド時に AST 突合、不一致でビルド中止）。ヒアリング様式・台帳テンプレ改訂時は check_docs.py のフィンガープリント/ヘッダー定数を再実測して更新。提出書類要件の一次裏取りは `reference/必要書類マトリクス.md`（公募要領＋交付申請マニュアル、鮮度管理あり）。

## R215/R216 や賃金台帳に迷ったら

- まず `docs/補助金_実務知識ベース.md` を読む（公募要領と Codex 調査結果が集約済み）
- 公募要領の WebFetch / Codex のフル再調査は **本資料で判断できないとき** のみ
- 案件特有の事情（複数集計行の意味、賞与シートの有無など）は最終的にユーザーが PDF 原本で確認する運用

## 関連会議（議事フォルダ）
- [2026-08-06 ローカルCCでの補助金業務について（坂平さん）](../社内資料/会議用/議事/2026-08-06_坂平さん_ローカルCCでの補助金業務.md) — ローカル移行後の使用感・4次のスケジュール逆算・**ダブルチェックの持ち方**（機械ゲート／別セッションのAI／人しか判断できない前提条件の3層に仕分ける案）。
- [2026-07-27 補助金3次振り返りMTG](../社内資料/会議用/議事/2026-07-27_補助金3次振り返りMTG.md) — 4次＝**8/25締切**（資料提出8/19・セカンドオピニオン8/12は社内締切）。村上さんからセキュリティ加点・みなし大企業・法人成り・コンソーシアムの制度レクチャー → 一次資料で裏取りして [制度リファレンス](docs/補助金_制度リファレンス.md) を新設。**裏取りで判明した実害2件**: ①賃上げ3.5%が必須要件なのは「過去にIT補助金2022〜2025の交付決定を受けた事業者」で、ツールの3%固定だと要件未達になる（従来「150万未満・過去交付なしが3.5%」と誤認していた）②公式セカンドオピニオンマニュアルが7/22改訂され、評価軸②に「組織としてAIも積極的に活用していく姿勢」が追加されているが社内配布物に未反映。アカウント代理店変更は8/3（月）。
- [2026-07-27 週次定例（社長）](../社内資料/会議用/議事/2026-07-27_週次定例（社長）.md) — **ツール（Streamlit）の修正はほぼ止めて、コンテキスト整備を優先**している状態。**今後は各担当が自分のPCで補助金作業を行う方が正確な成果物が出る**という整理で、そこへ向けて知識・手順を整理して出す。**社内業務は坂平さんとメインで進める**（※「坂平さんと」が補助金ツール側を指すか社内業務の行を指すかは会話上あいまい・要確認。7/21 時点の「担当は一時的に田村さん」からの体制変化の可能性あり）。
- [2026-07-21 週次定例（社長）](../社内資料/会議用/議事/2026-07-21_週次定例（社長）.md) — 補助金業務のAIコンテキスト化（新任者がAIに聞きながら業務を進められる状態にする）を本日15:00の補助金チーム振り返りで相談。担当は一時的に田村さん、進捗50〜60%。※このStreamlitツール（書類自動生成）とは別軸の「業務ナレッジのAI化」の取り組み。
- [2026-07-21 補助金チーム振り返り](../社内資料/会議用/議事/2026-07-21_補助金チーム振り返り.md) — 実討議は①セカンドオピニオンのプロンプト整理（07B確認メール→06予約フォーム入力文の2本立て。07・06Bは廃止方向）②賃金台帳作成（PDF→Excel）の「一旦ローカル標準」化（精度優先。ツールは速さ/不慣れ者向け代替）③賃上げ計画値の丸めを四捨五入→切り上げへ変更（羽根対応）④台帳PDF丸ごと転記シートの追加（ローカル実装・羽根）⑤ローカル環境（引き継ぎパック＋スキル）の全員インストール（羽根が水木までに最新化）。事前準備の付録A/B（必要書類の必要十分条件・給与総額論点6件）とセカンドオピニオン未決3点は未討議＝持ち越し。
